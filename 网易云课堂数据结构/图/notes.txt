怎么用程序表示一个图：
1.邻接矩阵G[N][N], N = [0, N-1](闭区间)
	问题：对于有N个顶点的无向图，怎样存储可以省一半空间？
		答：用一个长度为N(N+1)/2的1维数组
	用一个长度为N(N+1)/2的一维数组A存储{G00,G10,G11,...,Gn-1n-1},则Gij在A中对应的下标是：
		(i*(i+1)/2+j)
	对于网络，只要把G[i][j]的值定义为边<Vi,Vj>的权重即可。
2.邻接表：G[N]为指针数组，对应矩阵每行一个链表，只存非零元素。（但是图要足够稀疏才合算）
	